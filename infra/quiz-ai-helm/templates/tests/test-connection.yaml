apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quiz-ai-helm.fullname" . }}-test-connection"
  labels:
    {{- include "quiz-ai-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quiz-ai-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
